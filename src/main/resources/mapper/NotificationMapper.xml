<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.arknow.notification.mapper.NotificationMapper">

    <insert id="insert">
        INSERT INTO notifications (id, user_id, type, actor_id, post_id, comment_id, created_at)
        VALUES (#{id}, #{userId}, #{type}, #{actorId}, #{postId}, #{commentId}, NOW())
    </insert>

    <select id="listByUser" resultType="com.arknow.notification.model.Notification">
        SELECT n.id, n.user_id, n.type, n.actor_id, n.post_id, n.comment_id,
               n.is_read, n.created_at,
               u.nickname AS actor_nickname, u.avatar AS actor_avatar,
               p.title AS post_title
        FROM notifications n
        JOIN users u ON n.actor_id = u.id
        LEFT JOIN know_posts p ON n.post_id = p.id
        WHERE n.user_id = #{userId}
        ORDER BY n.created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countUnread" resultType="int">
        SELECT COUNT(*) FROM notifications WHERE user_id = #{userId} AND is_read = 0
    </select>

    <update id="markRead">
        UPDATE notifications SET is_read = 1 WHERE user_id = #{userId} AND is_read = 0
    </update>

    <update id="markReadById">
        UPDATE notifications SET is_read = 1 WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM notifications WHERE id = #{id} AND user_id = #{userId}
    </delete>
    <!-- ==================== Admin ==================== -->

    <select id="listAll" resultType="com.arknow.notification.model.Notification">
        SELECT n.id, n.user_id, n.type, n.actor_id, n.post_id, n.comment_id,
               n.is_read, n.created_at,
               u.nickname AS actor_nickname, u.avatar AS actor_avatar,
               p.title AS post_title
        FROM notifications n
        JOIN users u ON n.actor_id = u.id
        LEFT JOIN know_posts p ON n.post_id = p.id
        ORDER BY n.created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM notifications
    </select>

    <delete id="deleteByIdAdmin">
        DELETE FROM notifications WHERE id = #{id}
    </delete>

    <insert id="insertSystem">
        INSERT INTO notifications (id, user_id, type, actor_id, post_id, comment_id, created_at)
        VALUES (#{id}, #{userId}, #{type}, #{actorId}, #{postId}, #{commentId}, NOW())
    </insert>

</mapper>
