<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.arknow.user.mapper.UserMapper">

    <select id="existsByPhone" resultType="boolean">
        SELECT COUNT(1) > 0 FROM users WHERE phone = #{phone} AND deleted_at IS NULL
    </select>

    <select id="existsByEmail" resultType="boolean">
        SELECT COUNT(1) > 0 FROM users WHERE email = #{email} AND deleted_at IS NULL
    </select>

    <select id="findByPhone" resultType="com.arknow.user.domain.User">
        SELECT * FROM users WHERE phone = #{phone} AND deleted_at IS NULL
    </select>

    <select id="findByEmail" resultType="com.arknow.user.domain.User">
        SELECT * FROM users WHERE email = #{email} AND deleted_at IS NULL
    </select>

    <select id="findById" resultType="com.arknow.user.domain.User">
        SELECT * FROM users WHERE id = #{id} AND deleted_at IS NULL
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO users (phone, email, password_hash, google_id, nickname, avatar, bio, tags_json, role)
        VALUES (#{phone}, #{email}, #{passwordHash}, #{googleId}, #{nickname}, #{avatar}, #{bio}, #{tagsJson}, #{role})
    </insert>

    <update id="updatePassword">
        UPDATE users SET password_hash = #{passwordHash}, updated_at = NOW() WHERE id = #{id}
    </update>

    <update id="updateProfile">
        UPDATE users SET
            nickname = #{nickname},
            bio = #{bio},
            gender = #{gender},
            birthday = #{birthday},
            zg_id = #{zgId},
            school = #{school},
            tags_json = #{tagsJson},
            updated_at = NOW()
        WHERE id = #{id}
    </update>

    <update id="updateAvatar">
        UPDATE users SET avatar = #{avatar}, updated_at = NOW() WHERE id = #{id}
    </update>

    <update id="updateTags">
        UPDATE users SET tags_json = #{tagsJson}, updated_at = NOW() WHERE id = #{userId}
    </update>

    <update id="updateEmail">
        UPDATE users SET email = #{email}, updated_at = NOW() WHERE id = #{id}
    </update>

    <update id="softDelete">
        UPDATE users SET deleted_at = NOW(), updated_at = NOW() WHERE id = #{id} AND deleted_at IS NULL
    </update>

</mapper>
