<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.arknow.auth.audit.LoginLogMapper">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO login_logs (user_id, identifier, channel, ip, user_agent, status)
        VALUES (#{userId}, #{identifier}, #{channel}, #{ip}, #{userAgent}, #{status})
    </insert>

    <!-- Admin methods -->
    <select id="listAll" resultType="com.arknow.auth.audit.LoginLog">
        SELECT id, user_id, identifier, channel, ip, user_agent, status, created_at
        FROM login_logs
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="int">
        SELECT COUNT(*) FROM login_logs
    </select>

    <select id="findById" resultType="com.arknow.auth.audit.LoginLog">
        SELECT id, user_id, identifier, channel, ip, user_agent, status, created_at
        FROM login_logs
        WHERE id = #{id}
    </select>
</mapper>
